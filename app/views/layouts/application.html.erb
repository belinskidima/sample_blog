<!DOCTYPE html>
<html>
<head>
  <title>Blog</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  </head>
<!--======================================================================-->
<body>
<div class="container">
<div class="row">
<div class="col-xs-12">
<nav class="menu">
  <h1 class="name"><i class="fi-widget"></i> MY BLOG</a></h1>
  <ul class="inline-list">
    <li><a href="/articles">Home</a></li>
    <li class="active"><a href="/articles/new">New Article</a></li>
    <li><a href="/contacts">Contacts</a></li>
  </ul>
  <ul class="inline-list hide-for-small-only account-action">
    <li><% if user_signed_in? %>
  <li style = "color:white">Hello, <%= current_user.username %> </li>
  <%= link_to  'Sign Out', destroy_user_session_path, :class => "signup", data: { confirm: 'Are you certain you want sign out?' }, :method => :delete %></li>
    <li class="">
    <% else %>
    <%= link_to  'Sign In', new_user_session_path, :class => "signup" %> 
   <% end %>
    </li>
  </ul>  
</nav>
</div>
</div>

<% if notice && notice.length > 0 || alert && alert.length > 0%>	
  <div class="row">
 <p class="notice"><%= notice %></p>
 <p class="alert"><%= alert %></p>
 </div>
 <% else %>
 <br/>	
<% end %>
<%= yield %>
</div>
</body>
<!--======================================================================-->
</html>
